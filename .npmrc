# 关闭非必要网络检查和进度噪声，是为了让安装输出只保留依赖变更与真正可处理的问题。
# Disable nonessential network checks and progress noise so install output contains only dependency changes and actionable problems.
fund=false
audit=false
update-notifier=false
progress=false
